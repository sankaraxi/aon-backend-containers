version: '3.8'

services:
  code-server:
    container_name: "code-server-a1l1q3-react-pac70"
    image: "sankarkg/level-three-test:latest"
    # image: "sankarkg/demo-test-one:latest"
    ports:
      - "10002:8080"
      - "10003:5173"
    volumes:
      - frontend-src-pac70:/home/coder/project/src
    environment:
      - WATCHPACK_POLLING=true
      # - WATCHPACK_POLLING=true
      # - PUID=1000
      # - PGID=1000
      # - TZ=Etc/UTC
      # - CONNECTION_TOKEN=
    command:
        code-server --bind-addr 0.0.0.0:8080 --auth none /home/coder/project
    networks:
      - my_network

  # nginx:
  #   image: nginx
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf
  #   ports:
  #     - "3005:80"
  #   depends_on:
  #     - code-server
      
      
networks:
  my_network:
    external: true

volumes:
  frontend-src-pac70:
    driver: local
 
# networks:
#   my_network:
#     driver: bridge
